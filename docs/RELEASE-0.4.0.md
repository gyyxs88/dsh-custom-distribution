# 0.4.0 验收记录

2026-09-12，Windows x64 / Node.js 24.19.0。

## 版本

- DeepSeek Harness：0.1.5-rc.2，上游提交 `fb2c4b9e698e30edb738bca4cf0618587db7d203`。
- Session Control：0.8.1；Remote Control：0.3.1；Code Agents：0.2.1。独立仓库已合并并发布，固定提交见 `manifest/release-lock.json`。
- 八个官方适配包、三个独立控制插件、at-file、GenUI 和本机服务控制，共十四个自有制品逐一固定 SHA-256。

## 验证

- 发行仓库：28 项测试通过；源码快照与打包结果核对通过。
- Session Control：81 通过、1 项平台跳过；Remote Control：74 通过、5 项平台跳过；Code Agents：179 通过、3 项平台跳过。三个插件的打包消费者检查通过。
- 严格 `npm ci --ignore-scripts` 构建通过。应用直接依赖精确锁定；Claude SDK 的八个平台可选包也固定版本，安装时只选择匹配的平台。
- 干净安装通过；浏览器认证交换返回 303，认证后页面和客户端资源返回 200，启动图包含必需插件。
- 服务控制重启后进程更换，已有 Cookie 可继续访问；模型目录、图片能力、思维档位及路由设置的模拟检查通过。
- 用历史 0.3.0 发行包安装、升级到 0.4.0，再回滚到 0.3.0：程序版本和合成数据一起恢复，升级后的数据另行保留。
- 验收前后现役 DSH 的进程、监听者和运行状态一致。
- 发布边界检查通过；包内不含本机凭据、会话历史、工作区和远程主机登记。编译元数据中的本机路径已去除。

验收使用本地合成数据和模拟模型目录，没有调用付费模型、真实代码代理或远端主机。回滚只接受与具体版本切换匹配的完整快照；缺少快照时拒绝降级。

应用与 profile 载荷摘要：

- app：`297bb5ab3796c423ff0c6adec52b33e606bc8d586dcb8569c3824101249b2480`
- profile：`1cc3ce1e37850330b2c6a14e93165d736c4da84b82e95edea35ff01c20c80710`

最终外层 ZIP 摘要随 Release 的 `.sha256` 文件提供。
